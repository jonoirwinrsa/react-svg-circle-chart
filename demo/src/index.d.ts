import * as React from 'react';
declare type Props = {
    size?: number;
    values: {
        angle: number;
        color: string;
    }[];
};
export declare const CircleChart: React.FC<Props>;
export default CircleChart;
